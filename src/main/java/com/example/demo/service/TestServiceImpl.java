package com.example.demo.service;

import com.example.demo.exception.ReservationNotAvailableException;

import java.time.LocalDate;

public class TestServiceImpl {
    public void make() throws ReservationNotAvailableException {
        //throw new ReservationNotAvailableException("reservation.getCourtName()", LocalDate.now(), 1);
    }
}
